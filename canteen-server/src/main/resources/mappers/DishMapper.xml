<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.canteen.dao.DishDAO">
    <select id="findById" parameterType="String" resultType="Dish">
        SELECT * FROM dish WHERE id = #{id}
    </select>

    <select id="findAll" resultType="Dish">
        SELECT * FROM dish ORDER BY menu_date DESC, id
    </select>

    <select id="findByMenuDate" parameterType="String" resultType="Dish">
        SELECT * FROM dish WHERE menu_date = #{menuDate}
    </select>

    <insert id="save" parameterType="Dish">
        INSERT INTO dish (id, name, price, description, menu_date)
        VALUES (#{id}, #{name}, #{price}, #{description}, #{menuDate})
        ON DUPLICATE KEY UPDATE
        name = #{name}, price = #{price}, description = #{description}, menu_date = #{menuDate}
    </insert>

    <delete id="delete" parameterType="String">
        DELETE FROM dish WHERE id = #{id}
    </delete>
</mapper>